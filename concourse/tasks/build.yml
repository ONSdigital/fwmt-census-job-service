platform: linux

image_resource:
  type: docker-image
  source: {repository: gradle, tag: 4.10-jdk11}

inputs:
  - name: fwmt-census-job-service

outputs:
  - name: build-output

run:
  user: root
  path: "fwmt-census-job-service/concourse/tasks/build.sh"
